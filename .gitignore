# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
badger/badger-*

# Test binary, build with `go test -c`
*.test
badger-test*/

# Output of the go coverage tool
*.out

# Local cross-compile artifacts (make cross-compile / make ci)
dist/

#darwin
.DS_Store
